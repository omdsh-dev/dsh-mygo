# Source Layout

`src/index.ts` 是 @r05en1cu/dsh-mygo-cli 的入口（命令分发与注册）；
`src/args.ts` / `src/render.ts` 分别是参数解析与确定性报告渲染；
`src/init.ts` 从 `assets/plugin-template/` 生成官方模板对齐骨架；
`src/invariant.ts` 是包级 invariant companion。
