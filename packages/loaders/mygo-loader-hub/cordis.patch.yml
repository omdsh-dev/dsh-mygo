# dsh-mygo-loader-hub bundle patch：hub 市场适配器行（dsh.bundle 原生形态）。
# 依赖 dsh-mygo 行提供的 pluginManager 服务（经 bundle 层序在 dsh-mygo 之后）。

- insert:
    - id: dsh-mygo-loader-hub
      name: '@r05en1cu/dsh-mygo-loader-hub'
