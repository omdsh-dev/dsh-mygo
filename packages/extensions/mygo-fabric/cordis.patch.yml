# dsh-mygo-fabric bundle patch：fabric 扩展治理壳行（dsh.bundle 原生形态）。
# 依赖 dsh-mygo 行提供的 pluginManager 服务（经 bundle 层序在 dsh-mygo 之后）。

- insert:
    - id: dsh-mygo-fabric
      name: '@r05en1cu/dsh-mygo-ext-fabric'
